import { Global } from '@emotion/react';

export const Fonts = () => (
  <Global
    styles={`
        @import url('https://fonts.googleapis.com/css2?family=Lexend+Deca:wght@400;600;900&display=swap');
      `}
  />
);
